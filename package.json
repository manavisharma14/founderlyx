{
  "name": "omniflow",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm --filter web dev",
    "dev:web": "pnpm --filter web dev",
    "prisma:generate": "pnpm --filter @repo/db prisma generate",
    "prisma:migrate": "pnpm --filter @repo/db prisma migrate dev",
    "prisma:studio": "pnpm --filter @repo/db prisma studio",
    "postinstall": "turbo run generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "turbo": "^2.6.1"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "prisma": "^6.19.0"
  }
}
